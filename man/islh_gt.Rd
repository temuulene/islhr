% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table-gt.R
\name{islh_gt}
\alias{islh_gt}
\title{Apply Island Health styling to a gt table}
\usage{
islh_gt(
  data,
  groupname_col = NULL,
  rowname_col = NULL,
  stubhead = NULL,
  title = NULL,
  subtitle = NULL,
  source_note = NULL,
  width = 1,
  embed_fonts = getOption("islh.embed_fonts", TRUE)
)
}
\arguments{
\item{data}{A gt table or data frame.}

\item{groupname_col}{Column whose values become row group headings. Data
frames only.}

\item{rowname_col}{Column whose values become row labels. Data frames only.}

\item{stubhead}{Header over the row labels. Defaults to the name of
\code{rowname_col}; use \code{""} for none.}

\item{title, subtitle}{Optional table heading.}

\item{source_note}{Optional source note.}

\item{width}{Table width as a fraction of the available text width.}

\item{embed_fonts}{Embed the installed BC Sans faces as data URLs. The
session default is set by \code{islh_setup()} and defaults to \code{TRUE} otherwise.}
}
\value{
A styled gt table.
}
\description{
Styles a data frame or an existing gt table with the Island Health header
band, rules and type.
}
\section{Grouped rows and row labels}{


\code{groupname_col} gathers rows under a heading for each value of that column,
such as one heading per health service delivery area. \code{rowname_col} turns
a column into row labels at the left of the table, set apart by a thin
rule. Both are passed to \code{\link[gt:gt]{gt::gt()}}, so they apply when \code{data} is a data
frame; for a table built with \code{gt::gt()} already, set them there. The row
label column keeps its name as a header unless \code{stubhead} says otherwise.
}

\examples{
if (requireNamespace("gt", quietly = TRUE)) {
  islh_gt(
    head(islh_example_data()),
    title = "Example encounters",
    source_note = "Source: simulated package data",
    embed_fonts = FALSE
  )

  # One heading per HSDA, with the site as the row label.
  sites <- data.frame(
    hsda = c("South", "South", "Central", "North"),
    site = c("Site A", "Site B", "Site C", "Site D"),
    opened = c("2017-02-01", "2018-06-18", "2017-09-11", "2020-06-01")
  )
  islh_gt(
    sites,
    groupname_col = "hsda",
    rowname_col = "site",
    stubhead = "Site",
    title = "Site details",
    embed_fonts = FALSE
  )
}

}
