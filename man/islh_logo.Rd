% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assets.R
\name{islh_logo}
\alias{islh_logo}
\title{Path to an Island Health logo file}
\usage{
islh_logo(
  lockup = c("horizontal", "stacked"),
  variant = c("full-colour", "dark-blue", "white", "black"),
  format = c("svg", "png")
)
}
\arguments{
\item{lockup}{Logo arrangement: \code{"horizontal"} or \code{"stacked"}.}

\item{variant}{Colour variant: \code{"full-colour"}, \code{"dark-blue"}, \code{"white"} or
\code{"black"}.}

\item{format}{File format: \code{"svg"} for anything scalable, \code{"png"} where a
raster is required.}
}
\value{
A file path.
}
\description{
The brand system has two lockups in four colour variants. Full-colour is the
primary variant for both lockups, and dark blue is a second primary for the
horizontal lockup. Use white on dark backgrounds. \strong{Black is for print
only} — do not use it on screen.
}
\details{
Only the vector (\code{svg}) and raster (\code{png}) files ship with the package. The
Illustrator sources and print PDFs live in the \code{islh-brand-standard}
repository.
}
\examples{
islh_logo()
islh_logo("stacked", "white", "png")
}
