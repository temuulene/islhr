% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map.R
\name{coord_islh_map}
\alias{coord_islh_map}
\title{BC Albers coordinates for Island Health maps}
\usage{
coord_islh_map(crs = 3005, datum = NA, expand = FALSE, ...)
}
\arguments{
\item{crs}{Coordinate reference system. BC Albers unless you have a reason.}

\item{datum}{Graticule datum. \code{NA} draws no graticule.}

\item{expand}{Expand the panel beyond the data.}

\item{...}{Additional arguments passed to \code{ggplot2::coord_sf()}, such as
\code{xlim} and \code{ylim} for an inset panel.}
}
\value{
A ggplot2 coordinate system.
}
\description{
Sets the coordinate reference system to BC Albers (EPSG:3005), the
provincial standard that BC Data Catalogue and BC Stats layers already use.
Latitude and longitude (EPSG:4326) stretches Vancouver Island sideways and
distorts area, so a map drawn in it misrepresents how large each local
health area is.
}
\details{
It also drops the graticule that \code{ggplot2::coord_sf()} draws by default and
turns off the expansion around the data, both of which are chart furniture
on a projected map.
}
\examples{
\dontrun{
ggplot2::ggplot(boundaries) +
  ggplot2::geom_sf() +
  coord_islh_map()
}
}
