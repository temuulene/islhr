% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quarto.R
\name{islh_use_quarto}
\alias{islh_use_quarto}
\title{Add the Island Health Quarto format to a project}
\usage{
islh_use_quarto(dir = ".", overwrite = FALSE)
}
\arguments{
\item{dir}{Project directory. Defaults to the working directory.}

\item{overwrite}{Replace files that are already there.}
}
\value{
The paths written, relative to \code{dir}, invisibly.
}
\description{
Copies the \code{islh-report} Quarto extension into \verb{dir/_extensions}. Quarto
only looks for extensions inside the project directory, so this has to be a
copy rather than a reference into the R library. Re-run it after upgrading
\code{islhr} to pick up format changes.
}
\details{
Afterwards, set the format in the document's front matter or \verb{_quarto.yml}:

\if{html}{\out{<div class="sourceCode yaml">}}\preformatted{format: islh-report-html    # or islh-report-docx
}\if{html}{\out{</div>}}
}
\examples{
project <- file.path(tempdir(), "example-report")
dir.create(project, showWarnings = FALSE)
islh_use_quarto(project)
}
