% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{islh_check_project}
\alias{islh_check_project}
\alias{print.islh_project_check}
\title{Check a report project's Island Health files against this package}
\usage{
islh_check_project(dir = ".", quiet = FALSE)

\method{print}{islh_project_check}(x, ...)
}
\arguments{
\item{dir}{Project directory. Defaults to the working directory.}

\item{quiet}{Suppress the summary message.}

\item{x}{An \code{islh_project_check} object.}

\item{...}{Ignored.}
}
\value{
A data frame of class \code{islh_project_check} with one row per file,
giving \code{path} and \code{status}. \code{status} is one of \code{"current"}, \code{"outdated"},
\code{"missing"}, \code{"modified"} or \code{"unverified"}.
}
\description{
Compares the Quarto extension, \verb{_brand.yml} and the logos in a project
against the versions this package ships, and reports which are up to date,
out of date, missing or edited locally. It reads only; use
\code{\link[=islh_update_project]{islh_update_project()}} to act on the result.
}
\details{
A project written by \code{\link[=islh_create_report]{islh_create_report()}} or updated by
\code{\link[=islh_update_project]{islh_update_project()}} carries a manifest recording what was installed.
That record is what separates a file that is merely out of date from one
somebody has edited. Without it, a file that differs from this version is
reported as \code{"unverified"}, because the two cannot be told apart.
}
\examples{
project <- file.path(tempdir(), "check-example")
islh_create_report(project, format = "html")

check <- islh_check_project(project, quiet = TRUE)
table(check$status)
}
