% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup.R
\name{islh_setup}
\alias{islh_setup}
\title{Configure Island Health themes for the current project or document}
\usage{
islh_setup(
  format = c("auto", "html", "docx", "plots"),
  tables = TRUE,
  embed_fonts = TRUE,
  base_size = 12,
  grid = c("y", "x", "both", "none"),
  set_knitr = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{format}{Output format. \code{auto} detects Quarto HTML and DOCX renders;
outside a render it selects plot-only setup.}

\item{tables}{Configure the table stack for HTML or DOCX output.}

\item{embed_fonts}{Embed BC Sans in HTML gt tables. This is ignored for
non-HTML output.}

\item{base_size}{Base ggplot font size in points.}

\item{grid}{Major ggplot gridlines to display.}

\item{set_knitr}{Use \code{ragg_png} for subsequent knitr chunks when available.}

\item{quiet}{Suppress the setup confirmation message.}
}
\value{
A description of the active setup, invisibly.
}
\description{
Configure Island Health themes for the current project or document
}
\section{Undoing it}{


Setup changes the session, not just the next plot, which is what a report
render wants and what an interactive session usually does not. The first
call records what it found, so \code{\link[=islh_reset]{islh_reset()}} can put it back, and
\code{\link[=with_islh]{with_islh()}} applies the theme around one block of code and restores the
session afterwards even if that code fails.

Only the first call of a session takes the record, so repeated setup calls
followed by one \code{\link[=islh_reset]{islh_reset()}} return to the state before the first of them.
}

\examples{
check <- islh_check("plots", quiet = TRUE)
check[c("format", "tables", "ok")]

\dontrun{
islh_setup()
islh_reset()
}

}
