% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reset.R
\name{islh_reset}
\alias{islh_reset}
\title{Undo \code{islh_setup()}}
\usage{
islh_reset(quiet = FALSE)
}
\arguments{
\item{quiet}{Suppress the confirmation message.}
}
\value{
\code{TRUE} invisibly when something was restored, \code{FALSE} when
\code{\link[=islh_setup]{islh_setup()}} had not run.
}
\description{
Puts the session back the way \code{\link[=islh_setup]{islh_setup()}} found it: ggplot2's theme, the
discrete scale options, the geom defaults, knitr's graphics device, the
\verb{islh.*} options and, where they were configured, the \code{flextable} defaults
and the \code{gtsummary} theme.
}
\details{
The record is taken by the first \code{\link[=islh_setup]{islh_setup()}} call of the session, so
calling setup several times and then resetting once returns to the state
before the first of them, not to the state between two of them.

Use \code{\link[=with_islh]{with_islh()}} instead when the theme is only wanted around one block of
code. It cannot be left applied by an error part way through.
}
\examples{
# Nothing to undo in a session that has not called islh_setup().
islh_reset(quiet = TRUE)
}
