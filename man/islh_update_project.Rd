% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{islh_update_project}
\alias{islh_update_project}
\title{Update a report project's Island Health files}
\usage{
islh_update_project(
  dir = ".",
  dry_run = FALSE,
  backup = TRUE,
  force = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{dir}{Project directory. Defaults to the working directory.}

\item{dry_run}{Report what would change and write nothing.}

\item{backup}{Copy each replaced file into \verb{_islh-backup} first.}

\item{force}{Also replace files that were edited locally, or that the
project has no record of installing.}

\item{quiet}{Suppress the summary message.}
}
\value{
A data frame with one row per file, giving \code{path}, \code{status} and the
\code{action} taken: \code{"written"}, \code{"skipped"} or \code{"protected"}. Returned
invisibly.
}
\description{
Replaces the Quarto extension, \verb{_brand.yml} and the logos in a project with
the versions this package ships, so an existing report picks up a format fix
without being scaffolded again.
}
\details{
Files that were edited in the project are left alone. So is any file the
project has no record of installing, since an edit and an old version cannot
be told apart without one. Pass \code{force = TRUE} to replace those too; the
backup is what makes that recoverable.

Every file that is replaced is copied first into a timestamped folder under
\verb{_islh-backup}, unless \code{backup = FALSE}.
}
\examples{
project <- file.path(tempdir(), "update-example")
islh_create_report(project, format = "html")

# Nothing to do in a project that was just created.
islh_update_project(project, dry_run = TRUE, quiet = TRUE)
}
