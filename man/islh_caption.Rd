% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map.R
\name{islh_caption}
\alias{islh_caption}
\title{Build a map or figure caption from its required parts}
\usage{
islh_caption(
  source,
  extracted,
  boundary = NULL,
  standard_pop = NULL,
  suppression = NULL,
  governance = NULL,
  width = 100
)
}
\arguments{
\item{source}{Data source, for example \code{"BC Data Catalogue"}.}

\item{extracted}{Extraction date, as a string or a \code{Date}.}

\item{boundary}{Geography and boundary vintage, for example
\code{"Local health areas, 2024 boundaries"}.}

\item{standard_pop}{Standard population behind an age-standardised rate.}

\item{suppression}{Suppression rule applied before mapping.}

\item{governance}{Data governance statement, where one applies.}

\item{width}{Characters per line before wrapping. \code{Inf} leaves the caption
on one line.}
}
\value{
A single string for \code{ggplot2::labs(caption = )}.
}
\description{
A caption on a health figure has a job: it says where the numbers came
from, when they were pulled, what they are rates of, and which cells were
suppressed. Assembling it from named parts means no part is quietly left
out of a report.
}
\details{
Any argument left \code{NULL} is skipped, so a count map can omit
\code{standard_pop}. Give \code{governance} a data governance statement where the
geography covers First Nations communities.

ggplot2 does not wrap a caption, so a full one runs off the side of a
figure. The text is wrapped at \code{width} characters instead; 100 suits the
6.5-inch report preset in \code{\link[=islh_save_plot]{islh_save_plot()}}.
}
\examples{
islh_caption(
  source = "BC Data Catalogue",
  extracted = "2026-03-31",
  boundary = "Local health areas, 2024 boundaries",
  standard_pop = "2011 Canadian standard population",
  suppression = "Counts under 5 suppressed."
)
}
