% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install-deps.R
\name{islh_install_deps}
\alias{islh_install_deps}
\title{Install the packages your output format needs}
\usage{
islh_install_deps(
  format = c("auto", "html", "docx", "plots", "both"),
  quiet = FALSE
)
}
\arguments{
\item{format}{Output format to install for. \code{auto} detects Quarto HTML and
DOCX renders; outside a render it selects plot-only setup. \code{"both"}
installs the HTML and Word stacks together, for a project that renders to
either.}

\item{quiet}{Suppress the status message.}
}
\value{
The packages installed, invisibly.
}
\description{
Installs whatever \code{\link[=islh_check]{islh_check()}} reports as missing or out of date.
}
\details{
Island Health laptops block programs run from a user library, which breaks
installers that unpack with their own helper binary (\code{pak} is the usual
one), and they have no compiler, so a source build fails too. This uses base
R's \code{\link[utils:install.packages]{utils::install.packages()}} with \code{type = "binary"} on Windows and macOS,
which is the route that works there. Linux has no CRAN binaries, so it falls
back to the platform default.
}
\examples{
# Report what would be installed without installing it:
islh_check("html", quiet = TRUE)$missing
}
