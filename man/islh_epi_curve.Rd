% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi-curve.R
\name{islh_epi_curve}
\alias{islh_epi_curve}
\title{Draw an Island Health epidemic curve}
\usage{
islh_epi_curve(
  data,
  date,
  count,
  fill = NULL,
  facet = NULL,
  style = c("bars", "cases"),
  position = c("stack", "dodge"),
  labels = c("none", "total"),
  reference = NULL,
  reference_date = NULL,
  lower = "lower_limit",
  upper = "upper_limit",
  reference_mean = "reference_mean",
  show_year_lines = TRUE,
  bar_width = NULL,
  date_breaks = NULL,
  date_labels = NULL,
  max_cases = 50000L,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  x = NULL,
  y = "Cases",
  facet_scales = "fixed",
  aggregate = FALSE
)
}
\arguments{
\item{data}{A data frame containing one row per plotted group and period.}

\item{date}{Date column.}

\item{count}{Whole-count column.}

\item{fill}{Optional categorical column used to stack or dodge bars.}

\item{facet}{Optional column used to create small multiples.}

\item{style}{\code{"bars"} for routine surveillance or \code{"cases"} to draw one
outlined rectangle per case. Case tiles are intended for small outbreaks.}

\item{position}{Stack or dodge grouped bars. Case tiles require stacking.}

\item{labels}{Show no labels or total counts above each period.}

\item{reference}{Optional data frame containing a date column and one or more
of \code{lower_limit}, \code{upper_limit} and \code{reference_mean}.}

\item{reference_date}{Date column in \code{reference}. Defaults to the name used
by \code{date} when that column is present.}

\item{lower, upper, reference_mean}{Column names used for the reference ribbon
and line. Missing optional columns are ignored.}

\item{show_year_lines}{Draw a separator at the beginning of each new year.}

\item{bar_width}{Width in days. When \code{NULL}, the function infers it from the
distance between periods.}

\item{date_breaks, date_labels}{Values passed to \code{ggplot2::scale_x_date()}.
Useful defaults are selected when omitted.}

\item{max_cases}{Maximum number of rectangles allowed with
\code{style = "cases"}.}

\item{title, subtitle, caption, x, y}{Plot labels.}

\item{facet_scales}{Scale behavior passed to \code{ggplot2::facet_wrap()}.}

\item{aggregate}{Sum counts that share a date, fill group and facet. With
\code{FALSE}, the default, duplicated combinations are an error. See the input
grain section.}
}
\value{
A ggplot object. Additional ggplot2 layers and scales can be added in
the usual way.
}
\description{
\code{islh_epi_curve()} turns prepared period counts into a branded surveillance
figure. It deliberately keeps counting separate from plotting: use
\code{islhepi::islh_count_events()} first when starting from event-level data.
}
\section{Input grain}{


The function expects at most one row for each combination of date, fill
group and facet. Two rows for the same combination are not drawn as two
rows: \code{geom_col()} stacks them into a single taller bar, and \code{style = "cases"} draws overlapping tiles. Either way the figure is wrong and nothing
in it says so, which is why duplicates stop rather than plot.

Duplicates usually mean the data has not been counted yet, so the fix is
normally to count it: \code{islhepi::islh_count_events()} returns one row per
period and group, and fills in the periods with no events, which a plot
cannot infer. Use \code{aggregate = TRUE} when the rows really are separate
counts of the same period that should be added together. Columns outside the
date, fill and facet keep the first row's value.
}

\examples{
\dontshow{assign("font", "", envir = getFromNamespace(".islh_state", "islhr"))}
curve_data <- data.frame(
  week = seq(as.Date("2026-01-04"), by = "week", length.out = 12),
  cases = c(1, 2, 3, 5, 8, 13, 10, 7, 5, 3, 2, 1),
  source = rep(c("Community", "Facility"), 6)
)

islh_epi_curve(
  curve_data,
  date = week,
  count = cases,
  fill = source,
  title = "Reported cases by week"
)

}
