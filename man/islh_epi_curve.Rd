% Generated by roxygen2: do not edit by hand
\name{islh_epi_curve}
\alias{islh_epi_curve}
\title{Draw an Island Health epidemic curve}
\usage{
islh_epi_curve(
  data,
  date,
  count,
  fill = NULL,
  facet = NULL,
  style = c("bars", "cases"),
  position = c("stack", "dodge"),
  labels = c("none", "total"),
  reference = NULL,
  reference_date = NULL,
  lower = "lower_limit",
  upper = "upper_limit",
  reference_mean = "reference_mean",
  show_year_lines = TRUE,
  bar_width = NULL,
  date_breaks = NULL,
  date_labels = NULL,
  max_cases = 50000L,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  x = NULL,
  y = "Cases",
  facet_scales = "fixed"
)
}
\arguments{
\item{data}{A data frame containing one row per plotted group and period.}
\item{date}{Date column.}
\item{count}{Whole-count column.}
\item{fill}{Optional categorical column used to stack or dodge bars.}
\item{facet}{Optional column used to create small multiples.}
\item{style}{\code{"bars"} for routine surveillance or \code{"cases"} to
draw one outlined rectangle per case.}
\item{position}{Stack or dodge grouped bars. Case tiles require stacking.}
\item{labels}{Show no labels or total counts above each period.}
\item{reference}{Optional data frame containing a date and one or more of
\code{lower_limit}, \code{upper_limit} and \code{reference_mean}.}
\item{reference_date}{Date column in \code{reference}.}
\item{lower, upper, reference_mean}{Column names used for the reference ribbon
and line. Missing optional columns are ignored.}
\item{show_year_lines}{Draw a separator at the beginning of each new year.}
\item{bar_width}{Width in days. When \code{NULL}, inferred from the period spacing.}
\item{date_breaks, date_labels}{Values passed to \code{ggplot2::scale_x_date()}.}
\item{max_cases}{Maximum rectangles allowed with \code{style = "cases"}.}
\item{title, subtitle, caption, x, y}{Plot labels.}
\item{facet_scales}{Scale behavior passed to \code{ggplot2::facet_wrap()}.}
}
\value{A ggplot object that can receive ordinary ggplot2 layers and scales.}
\description{
Turns prepared period counts into a branded surveillance figure. Counting stays
separate from plotting; use \code{islhepi::islh_count_events()} when starting
from event-level data.
}
\examples{
\dontshow{assign("font", "", envir = getFromNamespace(".islh_state", "islhr"))}
curve_data <- data.frame(
  week = seq(as.Date("2026-01-04"), by = "week", length.out = 12),
  cases = c(1, 2, 3, 5, 8, 13, 10, 7, 5, 3, 2, 1),
  source = rep(c("Community", "Facility"), 6)
)

islh_epi_curve(
  curve_data,
  date = week,
  count = cases,
  fill = source,
  title = "Reported cases by week"
)
}
