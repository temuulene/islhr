on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

name: pkgdown

permissions: read-all

jobs:
  pkgdown:
    runs-on: ubuntu-latest
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-pandoc@v2

      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::pkgdown, local::.
          needs: website

      # The repo is private during development, so the site is built as an
      # artefact rather than published. Switch to deploy-pkgdown once the
      # repository is public and Pages is enabled.
      - name: Build site
        run: pkgdown::build_site_github_pages(new_process = FALSE, install = FALSE)
        shell: Rscript {0}

      - name: Check reference index is complete
        run: pkgdown::check_pkgdown()
        shell: Rscript {0}

      - uses: actions/upload-artifact@v4
        with:
          name: pkgdown-site
          path: docs
